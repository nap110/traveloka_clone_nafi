name: traveloka_clone
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter
  sqflite: ^2.0.0+4
  path: ^1.8.0
  flutter_map: ^4.0.0  # Downgrade this to ^4.0.0
  latlong2: ^0.8.2  # Keep this as ^0.8.2
  shared_preferences: ^2.3.5
  cupertino_icons: ^1.0.8
  share_plus: ^10.1.1
  flutter_rating_bar: ^4.0.1


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
